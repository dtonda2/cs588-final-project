# CONVEX_DEPLOYMENT specifies the current Convex deployment identifier.
# In this case, it indicates a development deployment using the name "energized-rooster-86".
# It also includes metadata about the team (empty here) and the project identifier ("signal-d80ea").
CONVEX_DEPLOYMENT=dev:energized-rooster-86 # team: Dhruvraj Tondanoorthy, project: signal-d80ea

# NEXT_PUBLIC_CONVEX_URL is the public URL for the Convex backend.
# The "NEXT_PUBLIC" prefix ensures that this variable is exposed to the client-side in Next.js.
# It points to the Convex deployment specified above.
NEXT_PUBLIC_CONVEX_URL=https://energized-rooster-86.convex.cloud

# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY is the public API key for Clerk.
# This key is safe to expose to the client and is used for authenticating Clerk on the frontend.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_cmFwaWQtdHVydGxlLTU0LmNsZXJrLmFjY291bnRzLmRldiQ

# CLERK_SECRET_KEY is the secret API key for Clerk.
# This key must be kept private and is used on the server-side to authenticate sensitive Clerk operations.
CLERK_SECRET_KEY=sk_test_4fAcDVBwl1oSxkflIop7WhhtF9lzrxg2jg0WklPz5X

# CLERK_WEBHOOK_SECRET is used to verify incoming webhook requests from Clerk.
# It ensures that the webhooks received are genuine and have not been tampered with.
CLERK_WEBHOOK_SECRET=whsec_WTeEN4IN80QhjBNDOLf2YfHf8MKZh1GF

# NEXT_PUBLIC_SUPABASE_URL is the public URL for the Supabase backend.
# It is used by the frontend to connect to the Supabase project.
NEXT_PUBLIC_SUPABASE_URL=https://ejdrvqoexhtwzyftsekn.supabase.co

# NEXT_PUBLIC_SUPABASE_ANON_KEY is the anonymous public key for Supabase.
# This key allows the client to interact with the Supabase API in a controlled manner, respecting the defined security rules.
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVqZHJ2cW9leGh0d3p5ZnRzZWtuIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDMwNTgyNjksImV4cCI6MjA1ODYzNDI2OX0.Tq7XkHm-8zJXr97TbCF91ZQQt_Py0fX-w5WxNeeVGVg

# NEXT_PUBLIC_PUSHER_KEY is the public API key for Pusher.
# It is used by the client to authenticate and connect to the Pusher service.
NEXT_PUBLIC_PUSHER_KEY=12e789b0a72b42826fcc

# NEXT_PUBLIC_PUSHER_CLUSTER indicates which Pusher cluster to use.
# The cluster helps route connections to the nearest server for optimal performance.
NEXT_PUBLIC_PUSHER_CLUSTER=mt1

# PUSHER_APP_ID is the unique identifier for the Pusher application.
# This value is used in server-side configurations to reference the correct Pusher instance.
PUSHER_APP_ID=1965335

# PUSHER_KEY is the secret key used for authenticating server-side operations with Pusher.
# It should be kept secure and not exposed to the client.
PUSHER_KEY=12e789b0a72b42826fcc

# PUSHER_SECRET is the secret token used to secure Pusher communications.
# It is essential for signing and verifying requests between your server and Pusher.
PUSHER_SECRET=6919642ff7a1f19db16e

# PUSHER_CLUSTER (redundant with NEXT_PUBLIC_PUSHER_CLUSTER) may be used in server-side settings to define the cluster.
# This ensures that both server and client are aligned in connecting to the correct Pusher cluster.
PUSHER_CLUSTER=mt1

# LIVEKIT_API_KEY is the public API key for LiveKit.
# LiveKit is used for real-time audio/video communications, and this key is necessary for client interactions.
LIVEKIT_API_KEY=APIrE86UWG2vvBC

# LIVEKIT_API_SECRET is the secret key for LiveKit.
# It is used on the server-side to generate tokens or authenticate operations with the LiveKit API.
LIVEKIT_API_SECRET=prkCWB0teAmgUNV2UbH2ytAxU4jSfy6NpOJWAvE4V43

# NEXT_PUBLIC_LIVEKIT_URL is the WebSocket URL for connecting to LiveKit.
# The "NEXT_PUBLIC" prefix ensures that this URL is accessible on the client side.
NEXT_PUBLIC_LIVEKIT_URL=wss://signal-a7gjxtqf.livekit.cloud


NEXT_PUBLIC_LOG_KEY=yRTr/e/RR4bZAmBfCTb8k6+TzpeiVnye3gkfnwzLNxpFWpElerwvYli0EGcVW4qMCWcYa8U+YTY6/1vq3WvNFA